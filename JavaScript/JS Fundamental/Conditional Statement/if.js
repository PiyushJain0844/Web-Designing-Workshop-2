let age = 25;

if (age >= 18 ) {
    console.log("You can vote");
} 

if (age <= 18 ) {
    console.log("You cannot vote");
} 

// Output -- You can vote


let mode = "dark";
let color;

if(mode === "dark") {
    color = "black";
}

if(mode === "light") {
    color = "white";
} 

console.log(color);

// Output -- black